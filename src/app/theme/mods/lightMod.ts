import {Theme} from "@mui/material";

export const light: Partial<Theme['palette']> = {
  primary: {
    main: 'rgb(0,0,0)'
  },
  background: {
    default: 'rgb(240, 240, 240)'
  }
} as Theme['palette']
