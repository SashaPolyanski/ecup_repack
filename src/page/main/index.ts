import {Component} from "./component";
import {main} from "../../constants/pagePath.tsx";
import {PageConfig} from "../types";

export default {
  component: Component,
  path: main.mainRoot,
} satisfies PageConfig
