import {Theme} from "@mui/material";

export const dark: Partial<Theme['palette']> = {
  primary: {
    main: 'rgb(255,255,255)',
  },
  background: {
    default: 'rgb(23,28,38)',
  }
} as Theme['palette']
